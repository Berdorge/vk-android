pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "ItmoHack2023"
include ':app'
includeBuild('happy-easter') {
    dependencySubstitution {
        substitute module('happy.easter:metric') using project(':metric')
        substitute module('happy.easter:transformer') using project(':transformer')
    }
}
